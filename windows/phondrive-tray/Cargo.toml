[package]
name = "phondrive-tray"
version = "0.1.0"
edition = "2021"

[dependencies]
tray-icon = "0.19"
crossbeam-channel = "0.5"
dirs = "6.0"
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
winreg = "0.52"

[profile.release]
opt-level = "s"
lto = true
strip = true
